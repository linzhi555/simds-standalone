centerTest:
	go run .
	make analyse
dcssTest:
	go run . -dcss
	make analyse

folder= target/$(shell date '+%m_%d_%H_%M_%S')
analyse:
	mkdir -p $(folder)
	rm -f target/*.log && rm -f target/*.png 
	analyse 


fmt:
	gofmt -l -w .
